export declare const columns: {
    verifyNeighboors: (row: Array<number>, index: number, skipCurrentIndex?: boolean) => number;
    numberifyColumns: (minefields: Array<number[]>) => Array<number[]>;
};
export default columns;
